using UnityEngine;

public class NPC_Frog : NPC
{
    public override void CustomNPCCode()
    {
        // if player has flute and "F_fluteAsk" active and "F_fluteGave" not active, then take flute and activate flag "F_fluteGave"
        // else if ...
        // else if ...
        // else do nothing
    }
}
